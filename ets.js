let btn = document.querySelector('#listar');
console.log(btn);




//const fs = require("fs");

/*async function pegar_todas_anotacoes(){
    let url = "http://localhost:3000/all"

    return fetch('http://localhost:3000/all',{
        method: 'GET',})
    
    .then((res)=> res.json())
    .then(res2 =>{
        fs.writeFile('lista.json', JSON.stringify(res2, null, 2), err=>{
            if(err) throw new Error("algo está errado")
            console.log('tudo certo')
        });
    })
}
pegar_todas_anotacoes();*/
